import { initUserManagement } from "./userManagement.js";

/* ============================================================
   INIT
   ============================================================ */
window.addEventListener("DOMContentLoaded", initUserManagement);
